Now make a service layer to make the logic to handle the data operation

touch StudentService inside the servies make an interface

make a new package as impl for the implementations
make the student service implementation into that

stream has map and filter that will transform one collection to other collection