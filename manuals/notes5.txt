inside the entity package make the Students class.

then we will make the code inside the students.java

there will be @Id annotation and @Generate id that will make the 
id by itself , with the user to write the logic.

according the database , it will be generating id if set as auto.
if it is written as Identity, it will by default make it by 1 2 and so on.

now run the code and you will see that the table will be made according to the 
fields in the class.


-----------------logs ---------------------
2026-02-22T12:24:39.928+05:30  INFO 4056 --- [LearningRestApis] [           main] org.hibernate.orm.connections.pooling    : HHH10001005: Database info:
	Database JDBC URL [Connecting through datasource 'HikariDataSource (HikariPool-1)']
	Database driver: undefined/unknown
	Database version: 18.2
	Autocommit mode: undefined/unknown
	Isolation level: undefined/unknown
	Minimum pool size: undefined/unknown
	Maximum pool size: undefined/unknown
2026-02-22T12:24:42.140+05:30  INFO 4056 --- [LearningRestApis] [           main] o.h.e.t.j.p.i.JtaPlatformInitiator       : HHH000489: No JTA platform available (set 'hibernate.transaction.jta.platform' to enable JTA platform integration)
Hibernate: 
    create table student (
        id bigint generated by default as identity,
        email varchar(255),
        name varchar(255),
        primary key (id)
    )
-----------------logs ---------------------

you see that the hibernate made the table for you

we made the table for you...

Student object is made according to the class.
hibernate makes the object into relation/ table of the database.
this is known as the ORM : Object Relational Mapping.

Now we need an interface that operates upon the tables.
this is the repository layer.

make an interface : studentRepository
all the data access /manipulation query lies here
and extend it with Jpa repository that will extend the crud repository

now give it the annotation of @Repository

then connect the repository with the controller.

go to student controller.java comment the url /student one.